{
  "name": "food-order-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && yarn start\"",
    "start": "concurrently \"cd server && npm run dev\" \"cd client && yarn start\"",
    "db": "cd && /Users/Ryan/mongodb/bin/mongod --dbpath=/Users/Ryan/mongodb-data/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  },
  "dependencies": {}
}
