import React from 'react';
import Styled from './styles';

const Logo = () => {
    return (
        <Styled.Logo>
            <h2>People's Market</h2>
        </Styled.Logo>
    );
};

export default Logo;