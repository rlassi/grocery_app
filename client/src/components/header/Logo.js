import React from 'react';
import Styled from './styles';

const Logo = () => {
    return (
        <Styled.Logo>
            <h1>People's Market</h1>
        </Styled.Logo>
    );
};

export default Logo;